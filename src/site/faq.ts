/**
 * FAQ du site : page d'accueil (Schema.org) et source des questions.
 * Contenu provisoire vide en attendant la rédaction Brut vers Net.
 */
export const faq = [] as { question: string; answer: string }[];
